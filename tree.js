class Tree{
  constructor(persons){
    //takes in an array of Person objects and creates a directed graph
    //directed graph consists of nodes
    //one node represents one person
    //node also keeps track of the relative position in the hierarchy
    //the and which parent node it is linked to and which nodes are its 
    //children
  }
}